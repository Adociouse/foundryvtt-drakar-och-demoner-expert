<div class="dode-roll-card {{cssClass}}">
  <header class="roll-card-header">
    <strong>{{label}}</strong> — FV {{fv}}
  </header>
  <div class="roll-card-result">
    <span class="d20-result">{{result}}</span>
    <span class="outcome-label">{{outcomeLabel}}</span>
  </div>
</div>
